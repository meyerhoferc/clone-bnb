<!DOCTYPE html>
<html lang="en" class="gr_getbootstrap_com">
<head>

    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="description" content="">
    <meta name="author" content="">

    <title>Portfolio Item - Start Bootstrap Template</title>
    <link href="css/bootstrap.min.css" rel="stylesheet">
    <link href="css/portfolio-item.css" rel="stylesheet">
</head>
  <body data-gr-c-s-loaded="true">
      <div class="container">
          <div class="row">
              <div class="col-lg-12">
                  <h1 class="page-header"><%= @listing.title %>
                  </h1>
              </div>
          </div>
          <div class="row">
              <div class="col-md-8">
                <a href="#">
                 <img class="img-responsive portfolio-item" src="<%= @listing.images[0].image_url %>" alt="">
               </a>
             </div>
              <div class="col-md-3">
                  <h3>About this listing:</h3>
                  <p><%= @listing.description %></p>
              </div>
              <div class="col-md-1">
                <h2>$<%= @listing.cost_per_night %>/night</h2>
                <%= link_to "Book Now", new_listing_reservation_path(@listing), class: "btn btn-success" %></td>
              </div>
              <div class="col-sm-3 col-xs-6 amenities">
                  <h3>Amenities at this property</h3>
                  <% @listing.amenities.each do |amenity| %>
                    <ul>
                      <%= amenity.name %>
                    </ul>
                  <% end %>
              </div>
          </div>
          <div class="row">
              <div class="col-lg-12">
                  <h3 class="page-header"></h3>
              </div>
              <div class="col-sm-3 col-xs-6">
              </div>
              <div class="col-sm-3 col-xs-6">
                <a href="#">
                    <img class="img-responsive portfolio-item" src="<%= @listing.images[2].image_url %>" alt="">
                </a>
              </div>
          </div>
          <div class="row">
              <div class="col-lg-12">
                  <h3 class="page-header">About the Host</h3>
              </div>

              <div class="col-sm-3 col-xs-6">
                  <img class="img-circle img-thumbnail img-responsive portfolio-item" src="<%= @listing.user.avatar.url(:thumb) %>">
                  <h4> <%= @listing.user.first_name %> </h4>
              </div>
              <div class="col-sm-1 col-xs-1">
                <% if current_user.present? %>
                <%= link_to "Message #{@listing.user.first_name}", conversations_path(initiator_id: current_user.id, recipient_id: @listing.user_id), method: :post %>
                <% end %>
              </div>
              <div class="col-sm-3 col-xs-6">
                  <h3>About the Host</h3>
                  <p>  <%= @listing.user.about_me %> </p>
              </div>

          </div>
          <div>
            <% if @reservation_listing %>
              <%= link_to "Write a Review", listing_reviews_new_path(@listing) %>
            <% end %>
            <%= render partial: "shared/review" %>
          </div>


          <hr>
          <footer>
              <div class="row">
                  <div class="col-lg-12">
                    <p>Copyright © CloneBnB 2017</p>
                  </div>
              </div>
          </footer>

      </div>
      <script src="js/jquery.js"></script>
      <script src="js/bootstrap.min.js"></script>
  </body>
</html>
